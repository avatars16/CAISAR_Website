
<br>
<h1>Edit members: <%- committee.committeeName%></h1>
<%  let notShowForMembers = ["committeeId","userId","committeeURL", "startDate","endDate"]
for (let member of committeeMembers) {
        if(member.userId){
        %>
<form action="/committees/<%=committee.committeeURL %>/members/edit?_method=PUT" method="post">
                <%=member.firstName%>
                 <a href= "/users/<%=member.userURL%>/"><%=member.userURL%></a>
                <br>
                <fieldset>
                    <label for= "memberRole" > Member role </label>
                    <input type="text" name= "memberRole" id= "memberRole" value = <%= member.memberRole%>>
                </fieldset>
                <fieldset>
                    <label for= "startDate" > Start data </label>
                    <input type="date" name= "startDate" id= "startDate" value = <%= member.startDate%>>
                </fieldset>
                <fieldset>
                    <label for= "endDate" > End date </label>
                    <input type="date" name= "endDate" id= "endDate" value = <%= member.startDate%>>
                </fieldset>
    <button type="submit">Change</button>
</form>
<% if(deletePermission) {%>
<form action= "/committees/<%=committee.committeeURL%>/members/edit?user=<%=member.userId%>&_method=DELETE" method="post">
    <button type="submit">Delete</button>
</form>
<%}%>
<% } } %> 

<form action="/committees/<%=committee.committeeURL %>/members/edit" method="post">
    <label for="userURL">userURL</label>
    <input type="userURL" name="userURL" id="userURL" onkeyup="sendData(this)">
    <button type="submit">Add user to this committee</button>    
</form>
<section id="searchResults"></section>
<ul id="ulSearchResults" onclick="getUserInLi(event)"></ul>
<script type="text/javascript" src="/js/searchuserURL.js"></script>
<br>
